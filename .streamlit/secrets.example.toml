# Streamlit Community Cloud: paste this into the app's "Secrets" settings
# (Settings -> Secrets). Locally you can copy it to .streamlit/secrets.toml
# but the .env file works too.
GEMINI_API_KEY = "your_primary_key"
GEMINI_API_KEY_2 = "optional_fallback_key_from_another_project"
